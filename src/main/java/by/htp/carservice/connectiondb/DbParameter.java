package by.htp.carservice.connectiondb;

/**
 * The Enum DbParameter.
 */
public enum DbParameter {
    
    /** The db driver. */
    DB_DRIVER,
    
    /** The db url. */
    DB_URL,
    
    /** The db user. */
    DB_USER,
    
    /** The db password. */
    DB_PASSWORD,
    
    /** The db poolsize. */
    DB_POOLSIZE,
    
    /** The db autoreconnect. */
    DB_AUTORECONNECT,
    
    /** The db characterencoding. */
    DB_CHARACTERENCODING,
    
    /** The db useunicode. */
    DB_USEUNICODE,
    
    /** The db usessl. */
    DB_USESSL
}
